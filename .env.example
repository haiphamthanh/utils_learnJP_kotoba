NODE_ENV=development
HOST=127.0.0.1
PORT=8000
TRUST_PROXY=false

# Optional but strongly recommended when publishing to a real host.
# When both values are set, every page/API except /api/health requires login.
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=learn_jp_wordlist
